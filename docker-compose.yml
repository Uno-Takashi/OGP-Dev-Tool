services:
  typescript:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ogp-dev-tool
    command: pnpm run watch
    tty: true
    volumes:
      - ./:/app
      - /app/node_modules
