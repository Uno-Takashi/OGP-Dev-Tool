Start webpack in watch mode for development.

**In devcontainer (standard):**

```
npm run watch
```

**On local machine without devcontainer:**

```
docker compose up
```

Execute the appropriate command to start the dev build with file watching. Output is streamed continuously — notify the user when the initial build completes.
