Run the production build and report the result.

**In devcontainer (standard):**
```
npm run build
```

**On local machine without devcontainer:**
```
docker compose run --rm typescript npm run build
```

Execute the appropriate command and report the result. If the build fails, show the error output and identify the cause.
